# Dependencies for scripts/generate_badges.py
#
# cairosvg also needs the native Cairo library:
#   Ubuntu/Debian: sudo apt install libcairo2
#   macOS:         brew install cairo
cairosvg>=2.7
Pillow>=10.0
